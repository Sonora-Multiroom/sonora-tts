# Upstream snapshots (read-only)

Copies of the `multiroom-ai` documents that define the contract this extension is built against,
taken 2026-09-20 so an agent working here does not have to reach into the host repository for the
basics.

They are **snapshots, not sources**. The living versions are in
`D:\projects-multiroom\multiroom-ai\docs\extensions\`; if the two disagree, the host repository is
right. Refresh them when the extension contract changes.
